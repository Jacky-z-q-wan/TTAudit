# D1-F-End：真实协作消息分布指纹的最终攻击修复与判决

## ——只修复无效攻击，不改研究假设，不再追加实验

**版本：**v1.0  
**日期：**2026-09-09  
**对应方案：**`D1_F.md`  
**已有结果：**`outputs/d1_f_preflight_20260909_v2/`  
**本阶段性质：**攻击实现的最后一次技术修复  
**最终结果只允许：**“D1-F 正式立项”或“A2/D1/D1-F 全面归档”

---

## 0. 当前已经确定的事实

以下结果已经通过，不再重复搜索或修改门槛。

### 0.1 官方完整数据与方法复现通过

本次已经使用 Latte 作者发布的完整缓存：

- CIFAR-100-C-Full：19种扰动、57个客户端、客户端样本总和60000；
- VLCS：4个域、40个客户端、客户端样本总和10729；
- 两个数据集均无跨客户端原始索引重叠；
- 官方仓库提交固定为 `7c2e02501e56e86dc6fa9e78ec606201a9674c55`；
- 模型固定为 CLIP ViT-B/16；
- 完整运行5个随机种子。

五种子均值为：

|数据集|不适应基础模型|Latte|相对基础模型提升|
|---|---:|---:|---:|
|CIFAR-100-C-Full|38.7867%|39.4557%|+0.6690个百分点|
|VLCS|80.8183%|82.5557%|+1.7373个百分点|

两个数据集均为5/5个种子正向提升，且与论文结果高度一致。官方复现门槛 R1 已通过。

### 0.2 跨客户端消息效用通过

完整 Latte 相对“只使用本地记忆”的结果为：

|数据集|跨客户端消息增益|以客户端为单位的95%置信区间|正向种子|
|---|---:|---:|---:|
|CIFAR-100-C-Full|+0.3584个百分点|[+0.3007, +0.4160]|5/5|
|VLCS|+0.9615个百分点|[+0.7491, +1.1735]|5/5|

因此跨客户端消息确实带来稳定效用，R2 已通过。

### 0.3 被动记录等价性通过

VLCS 种子0上：

- 开启/关闭消息记录后的准确率完全相同；
- 所有预测一致；
- 最大输出数值误差为0；
- 样本顺序一致；
- 消息数量与官方同步次数一致。

因此 R3 已通过。

### 0.4 G1 已经锁定为通过

由于 R1、R2、R3 均通过：

> **G1：真实方法有效且真实跨客户端消息有用——通过。**

本阶段不得重新计算或改变 G1，不得通过重新选择数据、参数或种子提高效用数字。

---

## 1. 为什么现有攻击结果不能作最终判决

现有结果中三个关键问题属于攻击实现错误，而不是有效的科学负结果。

### 1.1 空类别槽位产生非数值

现有 `messages/wire_payload.npz` 中：

- 类别原型含1,927,680个非数值；
- 不确定性含3,765个非数值；
- 二者正好对应3,765个尚未建立原型的空类别槽位。

结果导致所有“只看真实消息”的匿名跨时间关联攻击报错：

```text
Input contains NaN
```

这表示攻击没有运行成功，不能解释为“攻击失败”。

### 1.2 CIFAR 攻击标签做错

原方案要求预测4种扰动家族：

- 噪声；
- 模糊；
- 天气；
- 数字处理。

随机基线应为25%。现有实现却预测19种具体扰动，随机基线变成 (1/19=5.263\%\)，混淆矩阵也是19×19。因此名为 `CIFAR_family` 的结果并不是家族推断，必须作废。

现有“留出一种扰动”实验仍要求分类器预测训练时从未出现的那个具体扰动类别，任务定义不成立。正确任务应是：

> 训练时不出现某个具体扰动，测试时仍判断它属于噪声、模糊、天气或数字处理中的哪一家族。

### 1.3 当前可复核消息不完整

现有统一消息文件形状为：

```text
原型：(13480, 5, 512)
```

13480条消息等于 VLCS 种子0的337轮 × 40客户端，说明当前统一文件只完整保存了 VLCS 种子0，未完整保存 CIFAR 100类原型和两个数据集的各个种子。

因此现有攻击数字虽然有输出，但无法从统一保存的真实消息独立复核全部数据集与种子。

### 1.4 当前可保留的有效负结果

VLCS 当前纯消息域推断为25%，等于四分类随机基线。这是一个负面信号，但由于输入中存在空槽位非数值和错误预处理，本阶段将使用确定的缺失槽位编码原样复算一次。复算后不再增加攻击器或修改阈值。

VLCS 只看公开消息头时约75%，主要可能来自不同域的客户端总样本数和处理进度差异。该结果最多属于简单元数据侧信道，不满足 D1-F 的“真实原型内容泄露”主张，不能用于通过 G2。

---

## 2. 本阶段允许和禁止的操作

### 2.1 只允许四项修复

1. 将空类别槽位转换为有限的攻击输入，同时保留可从消息直接观察到的有效性掩码；
2. 将 CIFAR 标签正确映射为4种扰动家族；
3. 修正“留出具体扰动”的家族泛化任务；
4. 为两个数据集和必要种子保存可独立复核的真实消息、划分与逐样本/逐消息对预测。

### 2.2 禁止操作

- 不允许修改 Latte；
- 不允许改变官方数据划分；
- 不允许重新选择有利数据域；
- 不允许增加深层神经网络攻击器；
- 不允许根据测试结果选择攻击器或超参数；
- 不允许修改 G2/G3/G4 门槛；
- 不允许恢复原始 D1 的样本级成员推断主线；
- 不允许以公开客户端编号、域名称或私有诊断字段作为攻击输入；
- 不允许在本阶段之后再设计一次“最后实验”。

---

## 3. 阶段 A：生成可复核的真实消息记录

### 3.1 不需要重跑的内容

已有的官方基础模型、完整 Latte、只使用本地记忆和配对效用结果全部冻结。无需再次运行这些效用基线。

如现有消息不足，可仅运行开启被动记录的完整 Latte，用于生成攻击消息。该运行不重新决定效用门槛。

### 3.2 必须直接记录的官方输出

在官方 `upload_cache()` 返回位置立即执行：

```python
raw_prototype = prototype_to_upload.detach().clone()
raw_uncertainty = ents.detach().clone()
```

记录内容必须是客户端实际发送给服务器的原型和不确定性，不得从本地图片重新计算替代消息。

### 3.3 按数据集和种子分开保存

禁止再把类别数不同的 CIFAR 和 VLCS 消息拼进一个数组。使用：

```text
messages/
├── CIFAR100CFull/
│   ├── seed_0/
│   │   ├── raw_wire_payload.npz
│   │   ├── attack_ready_payload.npz
│   │   ├── public_header.jsonl
│   │   └── private_diagnostics.jsonl
│   └── ...
└── VLCS/
    ├── seed_0/
    │   ├── raw_wire_payload.npz
    │   ├── attack_ready_payload.npz
    │   ├── public_header.jsonl
    │   └── private_diagnostics.jsonl
    └── ...
```

### 3.4 固定抽取消息，避免存储全部通信

每个客户端的测试流按客户端自己的样本进度划分：

- 前25%：攻击训练时间段；
- 中间25%：攻击验证时间段；
- 后50%：未来测试时间段。

在每一时间段中，按等间距且预先确定的位置为每个客户端保存8条消息。不得根据消息内容、预测正确性或不确定性选择保存位置。

因此每个客户端最多保存24条消息。保存位置由数据长度和种子在运行前确定，并写入 `capture_schedule.json`。

### 3.5 原始消息和攻击输入必须同时保留

`raw_wire_payload.npz` 原样保留网络实际传输的非数值，因为非数值位置本身对攻击者可见。

对攻击器构造：

```python
valid_mask = (
    isfinite(raw_prototype).all(axis=-1)
    & isfinite(raw_uncertainty)
)

prototype_finite = nan_to_num(
    raw_prototype,
    nan=0.0,
    posinf=0.0,
    neginf=0.0,
)

uncertainty_finite = nan_to_num(
    raw_uncertainty,
    nan=1.0,
    posinf=1.0,
    neginf=1.0,
)
```

攻击输入包括：

- `prototype_finite`；
- `uncertainty_finite`；
- `valid_mask`。

使用 `valid_mask` 不属于读取私有标签，因为攻击者可以直接从收到的原始消息判断一个槽位是否为有限数值。

### 3.6 运行前单元测试

必须断言：

```text
raw payload hash == upload_cache 返回张量的序列化 hash
attack-ready prototype 中无非数值
attack-ready uncertainty 中无非数值
valid_mask 只由 raw payload 计算
攻击输入加载器不读取 private_diagnostics
CIFAR 每条原型形状 == (100, 512)
VLCS 每条原型形状 == (5, 512)
每数据集、每种子、每客户端、每时间段恰好8条消息
各时间段新输入图片索引重叠数 == 0
```

任一断言失败，只能修复记录/预处理代码后原样重跑，不能进入攻击。

---

## 4. 阶段 B：修复隐藏域与扰动家族推断

### 4.1 VLCS：未见客户端的四域推断

保留原来的客户端分组方式。对每个域的10个客户端：

- 6个攻击训练客户端；
- 2个攻击验证客户端；
- 2个最终测试客户端。

同一个客户端不能跨集合。五个种子只改变预先确定的客户端分组和 Latte 数据划分，不允许同一客户端同时出现在攻击训练与测试中。

目标标签为4个真实数据域：

```text
Caltech101
LabelMe
SUN09
VOC2007
```

随机基线为25%。

### 4.2 CIFAR：四种扰动家族映射

严格使用以下映射：

#### 噪声

```text
gaussian_noise
shot_noise
impulse_noise
speckle_noise
```

#### 模糊

```text
defocus_blur
glass_blur
motion_blur
zoom_blur
gaussian_blur
```

#### 天气

```text
snow
frost
fog
brightness
spatter
```

#### 数字处理

```text
contrast
elastic_transform
pixelate
jpeg_compression
saturate
```

将标签编码为：

```text
0 = 噪声
1 = 模糊
2 = 天气
3 = 数字处理
```

程序必须断言：

```text
unique(y_train) == {0,1,2,3}
unique(y_validation) == {0,1,2,3}
unique(y_test) == {0,1,2,3}
confusion_matrix.shape == (4,4)
随机基线 == 25%
```

若仍产生19×19混淆矩阵，直接判定实现无效。

### 4.3 CIFAR 客户端隔离

每种具体扰动有3个客户端。对每个种子随机但可复现地指定：

- 1个攻击训练客户端；
- 1个攻击验证客户端；
- 1个攻击测试客户端。

测试目标是扰动家族，不是具体扰动名称。

### 4.4 留出具体扰动的家族泛化

逐个将19种具体扰动作为留出扰动：

1. 攻击训练和验证阶段完全不包含该具体扰动的任何客户端；
2. 最终测试只使用该具体扰动的3个客户端；
3. 测试标签仍为其所属的4种扰动家族之一；
4. 训练集中该扰动家族必须还包含其他具体扰动；
5. 四个家族的攻击训练消息数通过下采样保持平衡。

例如留出 `gaussian_noise` 时，攻击训练仍包含 `shot_noise`、`impulse_noise` 和 `speckle_noise`，测试目标是判断 `gaussian_noise` 属于“噪声家族”，而不是要求模型输出训练阶段不存在的“gaussian_noise 类”。

### 4.5 三种观察面

只保留：

1. 只看真实消息内容；
2. 只看公开消息头；
3. 同时看两者。

纯消息内容由有限原型、不确定性和有效性掩码构成。主结论只能使用“只看真实消息内容”。

### 4.6 攻击器不变

继续使用预先指定的三个简单攻击器：

- 最近类别中心；
- 逻辑回归；
- 线性支持向量机。

训练特征标准化参数只从攻击训练集估计。攻击器和参数只根据验证客户端选择一次，最终测试集不得参与选择。

### 4.7 输出要求

每个数据集和种子保存：

- 训练、验证、测试客户端列表；
- 逐消息真实标签、预测标签和类别得分；
- 平衡准确率；
- 各类别平均F1分数；
- 4×4混淆矩阵；
- 客户端聚类重采样95%置信区间；
- 三个时间段的新输入图像索引 hash；
- 时间段之间重叠计数；
- 随机标签对照。

---

## 5. 阶段 C：修复匿名消息跨时间关联

### 5.1 任务不变

删除客户端身份字段后，判断两个不同时段的消息集合是否来自同一个客户端。

### 5.2 客户端隔离不变

以 VLCS 为主要数据：

- 辅助训练客户端：每域6个；
- 验证客户端：每域2个；
- 未见测试客户端：每域2个。

攻击训练、验证和测试客户端完全不重叠。

### 5.3 正负消息对

对每个未见测试客户端：

- 正样本：该客户端验证时间段与未来时间段的消息配对；
- 困难负样本：同一数据域内另一个未见客户端的未来消息；
- 正负数量严格相同；
- 不使用不同域客户端作为主要负样本，避免攻击器只做域识别。

配对规则和随机种子必须在计算攻击特征前写入 `pair_manifest.json`。

### 5.4 固定攻击预算

攻击训练阶段每个辅助客户端固定使用8条消息，不因 (q) 改变训练数据总量、攻击器容量或超参数搜索预算。

测试分别聚合：

\[
q\in\{1,2,4,8\}
\]

条消息。

### 5.5 有限的消息对特征

对两组消息分别在时间段内聚合，然后只在双方共同有效的类别槽位上计算：

- 每类原型余弦相似度；
- 每类原型差值的二范数；
- 不确定性绝对差；
- 共同有效类别比例；
- 仅一方有效的类别比例；
- 有效类别掩码的杰卡德相似度。

对得到的每类统计再计算：

- 均值；
- 标准差；
- 最小值；
- 最大值；
- 中位数。

若没有任何共同有效类别，余弦相似度和距离统计统一设为0，并通过“共同有效类别比例=0”显式表示，禁止生成非数值。

所有最终 pair 特征必须满足：

```text
isfinite(pair_features).all() == True
```

### 5.6 攻击器与选择方式

仍只使用：

- 最近类别中心；
- 逻辑回归；
- 线性支持向量机。

只允许通过验证客户端选择攻击器和参数，最终测试结果只能计算一次。

### 5.7 输出要求

对每个种子和每个 (q) 保存：

- 辅助、验证、未见测试客户端；
- 正负消息对来源；
- 逐对真实标签和预测分数；
- 曲线下面积；
- 平衡准确率；
- 误报率1%时的检出率；
- 以客户端为单位的95%置信区间；
- 随机配对标签对照；
- 纯消息、纯消息头、消息+消息头三种结果。

输出数组必须包含 `payload_predictions` 和 `payload_scores`，不能再次只保存 header 结果。

---

## 6. 阶段 D：控制实验与完整性检查

### 6.1 随机标签

- VLCS 四域推断应回到约25%；
- CIFAR 四家族推断应回到约25%；
- 匿名关联应回到约50%。

随机标签控制必须真正重新打乱标签并重新拟合攻击器，不能直接写入理论随机值。

### 6.2 纯消息头

VLCS 纯消息头可能因各域客户端样本数不同而预测成功。必须报告，但不能让它通过 G2。应额外移除以下直接规模字段后再作为诊断：

- 客户端总样本数；
- 当前处理进度；
- 剩余样本数；
- 可直接推导客户端域规模的字段。

该诊断只用于解释元数据侧信道，不替代纯消息攻击。

### 6.3 私有诊断隔离

在攻击入口打印实际加载字段，并断言不包含：

```text
client_id
domain
corruption_name
raw_label_histogram
sample_indices
same_client_label
```

这些字段只可在计算最终指标时作为真值使用。

### 6.4 重复性

每个攻击结果必须能由保存的：

- `attack_ready_payload.npz`；
- 分组划分；
- 消息对清单；
- 固定攻击配置；

在不重新运行 Latte 的情况下独立复算。

### 6.5 样本成员推断

沿用当前代表性负对照结果即可，不再扩大样本或重新扫描。现有结果约为0.519，只用于说明单样本成员泄露仍弱，不参与 D1-F 成败判定。

---

## 7. 最终门槛（不得修改）

### G1：真实协作方法有效

已经通过并冻结：

- R1 通过；
- R2 通过；
- R3 通过。

### G2：至少一种非平凡指纹攻击成立

以下至少一项必须满足：

#### 路径一：VLCS 隐藏域推断

- 只看真实消息内容的五种子平均平衡准确率 ≥65%；
- 以客户端为单位的95%置信区间下界 >55%。

#### 路径二：匿名跨时间关联

- 至少一个预注册 (q\in\{1,2,4,8\}) 的纯消息五种子平均曲线下面积 ≥0.65；
- 以客户端为单位的95%置信区间下界 >0.55；
- 该 (q) 必须在运行前预注册为主要比较点。默认主要比较点为 (q=4)，其他为风险曲线。

### G3：排除数据泄漏和元数据捷径

必须全部满足：

- 所有攻击输入均为有限数；
- 客户端训练/验证/测试集合互斥；
- 三个时间段的新输入图片索引无重叠；
- 攻击训练消息预算固定；
- 随机标签控制回到随机水平；
- 攻击输入不读取私有诊断字段；
- 主结论由纯消息内容支持；
- 结果不是由客户端总样本数或处理进度直接决定。

### G4：外部有效性

必须满足以下之一：

1. VLCS 四域推断和 CIFAR 四家族推断都达到与 G2 相同的65%/55%标准；
2. 隐藏域/家族推断与匿名跨时间关联两类攻击均达到标准。

留出具体扰动实验主要用于证明四家族攻击可以泛化到训练时未见的具体扰动。若普通四家族攻击通过而留出实验完全失败，应将外部有效性视为不足。

---

## 8. 最终二选一判决

### 8.1 D1-F 正式立项

仅当 G1、G2、G3、G4 全部通过时：

> D1-F 正式立项，研究问题确定为“真实协作测试时适应消息中的隐藏分布属性与匿名可关联性”。

通过只代表值得进入论文研究阶段，后续仍需解释指纹机制、增加第二种真实方法并设计防护。

### 8.2 A2/D1/D1-F 全面归档

出现以下任一情况即归档：

- 修复后 VLCS 纯消息域推断仍接近25%；
- CIFAR 正确四家族推断仍不稳定或接近25%；
- 匿名关联纯消息曲线下面积未达到0.65；
- 客户端置信区间下界未超过0.55；
- 只有公开消息头能够攻击；
- 结果只在单个种子成立；
- 留出具体扰动后失去泛化；
- 攻击再次因实现错误不能完成。

归档后：

- 不切换 CoLA；
- 不增加非线性攻击器；
- 不改变隐私目标；
- 不降低门槛；
- 不再追加实验；
- 转向新选题。

---

## 9. 推荐输出目录

```text
outputs/d1_f_end_YYYYMMDD/
├── decision.md
├── frozen_g1.json
├── implementation_checks.json
├── capture_schedule.json
├── messages/
│   ├── CIFAR100CFull/
│   │   ├── seed_0/
│   │   │   ├── raw_wire_payload.npz
│   │   │   ├── attack_ready_payload.npz
│   │   │   ├── public_header.jsonl
│   │   │   └── private_diagnostics.jsonl
│   │   └── seed_1_to_4/
│   └── VLCS/
│       └── seed_0_to_4/
├── hidden_domain/
│   ├── vlcs_group_splits.json
│   ├── cifar_family_mapping.json
│   ├── cifar_group_splits.json
│   ├── leave_one_corruption_out.json
│   ├── predictions.npz
│   ├── random_label_control.json
│   └── metrics.json
├── temporal_linking/
│   ├── client_splits.json
│   ├── pair_manifest.json
│   ├── predictions.npz
│   ├── random_label_control.json
│   └── metrics.json
└── sample_membership_negative_control/
    └── frozen_result.json
```

---

## 10. `decision.md` 必填模板

```text
# D1-F 最终判决

## 已冻结前提
R1 官方完整复现：通过
R2 跨客户端消息效用：通过
R3 被动记录等价性：通过
G1：通过

## 实现检查
CIFAR 标签类别数：4 / 错误
CIFAR 混淆矩阵形状：4×4 / 错误
VLCS 消息全部有限：是 / 否
CIFAR 消息全部有限：是 / 否
匿名关联 pair 特征全部有限：是 / 否
客户端集合互斥：是 / 否
时间段新输入图片无重叠：是 / 否
攻击预算固定：是 / 否
私有诊断未作为输入：是 / 否

## VLCS 隐藏域推断
纯消息五种子结果：
平均平衡准确率：
客户端95%置信区间：
纯消息头结果：

## CIFAR 四家族推断
纯消息五种子结果：
平均平衡准确率：
客户端95%置信区间：
留出具体扰动结果：

## 匿名跨时间关联
q=1：
q=2：
q=4（主要终点）：
q=8：
q=4客户端95%置信区间：

## 控制实验
随机标签：
只看规模字段：
类别槽位控制：

## 门槛
G1：通过
G2：通过 / 失败
G3：通过 / 失败
G4：通过 / 失败

## 最终决定
D1-F 正式立项 / A2-D1-D1-F 全面归档
```

`decision.md` 必须生成。不得因为结果不理想而省略最终判决。

---

## 11. 执行顺序与时间上限

### 第1天：修复与单元测试

- 实现空类别有限表示；
- 修正CIFAR四家族标签；
- 修正留出具体扰动任务；
- 检查攻击输入不读取私有字段；
- 用人工消息验证所有攻击器可运行。

### 第2天：按固定计划生成真实消息

- 为两个数据集和各必要种子保存固定24条/客户端消息；
- 保存原始消息和攻击就绪消息；
- 验证消息形状、hash和时间索引。

### 第3天：隐藏域与家族推断

- VLCS四域；
- CIFAR四家族；
- 留出具体扰动；
- 随机标签和消息头控制。

### 第4天：匿名跨时间关联

- (q=1,2,4,8)；
- 主要终点固定为 (q=4)；
- 保存逐对分数和客户端重采样区间。

### 第5天：复核与最终判决

- 从保存消息独立复算一次；
- 填写 `decision.md`；
- 根据 G1–G4 二选一；
- 不增加下一轮实验。

---

## 12. 最终纪律

这次工作的目的不是找到一个高攻击数字，而是让此前因非数值和错误标签而无效的攻击得到一次正确执行。官方方法和跨客户端收益已经成立；现在只问真实消息内容是否包含可跨客户端、跨时间泛化的非平凡分布指纹。修复后如果结果仍接近随机，就接受 D1-F 不成立并换题。

