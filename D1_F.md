# D1-F：协作测试时适应中的分布指纹泄露

## ——Latte 官方完整复现、真实消息审计与最终立项判决

**版本：**v1.0  
**日期：**2026-09-09  
**性质：**对上一次缩小规模终局实验的技术纠正，也是 D1-F 唯一一次正式立项验证  
**执行上限：**7–10 天  
**最终结果只允许：**“D1-F 正式立项”或“A2/D1/D1-F 全面归档”

---

## 0. 先说清楚：这不是原始 D1

原始 D1 已经结束。它原本研究：

1. 协作消息是否泄露某一张具体图片参与过适应；
2. 每条消息聚合的样本越少，单样本泄露是否越强；
3. 是否可以根据聚合样本数分配噪声和隐私预算。

已有实验已经表明：

- 样本成员推断接近随机；
- 每类样本上限与泄露没有稳定关系；
- 自制协作协议的效用不稳定。

这些结论不再重新打开。本文件研究的是由 D1 补充实验产生的新问题：

> Latte 等真实协作测试时适应方法传输的类别原型，是否会暴露客户端的隐藏数据域或敏感分布属性，或者使去除身份标识后的消息仍可被跨时间关联？

暂称该候选方向为 D1-F，其中 F 表示“指纹”。它只有在本文件规定的全部门槛通过后，才成为正式新项目。

---

## 1. 为什么允许这一次技术纠正

上一次 `outputs/end_experiment_20260909` 使用了 Latte 官方代码和正确版本，也证明被动记录钩子不会改变模型输出，但实验规模不是 Latte 官方完整设置：

- 一百类图像扰动实验只评测了2736张，而官方完整设置应覆盖60000张；
- 上次每个客户端只评测约48张，记忆方法尚未充分积累；
- 多域图像实验只使用12个客户端、评测576张，而官方设置是40个客户端、完整10729张；
- 因此，上次的“协作收益为负”只能说明缩小测试流下没有收益，不能作为官方完整 Latte 的有效复现。

本次只纠正以下技术条件：

- 恢复官方完整数据/官方缓存特征；
- 恢复官方客户端数量和划分函数；
- 使用完整测试流；
- 使用官方配置和固定代码版本；
- 保持原算法不变。

不得借技术纠正之名修改研究目标、挑选有利数据、降低攻击门槛或重新研究原始 D1。

---

## 2. 官方依据与固定版本

### 2.1 固定代码

- 官方仓库：`https://github.com/baowenxuan/Latte.git`
- 固定提交：`7c2e02501e56e86dc6fa9e78ec606201a9674c55`
- 模型：CLIP ViT-B/16
- 只允许在独立审计目录中编写被动记录子类或包装器；不得修改官方 Latte 数值逻辑。

### 2.2 官方数据规模

#### 一百类图像扰动数据

- 数据集名称：CIFAR-100-C-Full；
- 类别数：100；
- 扰动域数：19；
- 每个扰动域3个客户端；
- 总客户端数：57；
- 使用最高扰动强度；
- 完整评测样本数：60000；
- 同一原图的不同扰动版本不得落入不同客户端，官方划分函数已用于避免这种重叠。

#### 多域图像数据

- 数据集名称：VLCS；
- 类别数：5；
- 数据域数：4；
- 每个域10个客户端；
- 总客户端数：40；
- 完整样本数：10729；
- 每个域内部按类别进行分层划分。

### 2.3 官方论文数值锚点

使用 ViT-B/16 时，论文报告：

|数据集|不适应的基础模型|Latte|相对基础模型提升|
|---|---:|---:|---:|
|CIFAR-100-C|38.91%|39.51%|+0.60个百分点|
|VLCS|80.83%|82.57%|+1.74个百分点|

这些数值是5个随机种子的论文均值。它们只用于判断官方复现是否合理，不是我们的新贡献。

### 2.4 官方运行参数

#### CIFAR-100-C-Full

```text
每域客户端数 = 3
本地记忆每类容量 = 8
基础模型与记忆分类器权重 = 0.7
相似度放大系数 = 60.0
本地不确定性权重 = 1.5
外部记忆每类容量 = 5
原型 = 按不确定性指数加权平均
外部原型不确定性权重 = 1.5
```

#### VLCS

```text
每域客户端数 = 10
本地记忆每类容量 = 15
基础模型与记忆分类器权重 = 0.3
相似度放大系数 = 6.0
本地不确定性权重 = 6.0
外部记忆每类容量 = 12
原型 = 按不确定性指数加权平均
外部原型不确定性权重 = 6.0
```

---

## 3. 总体执行流程

严格按顺序执行：

```text
阶段一：官方完整效用复现
        ↓ 通过
阶段二：跨客户端协作的独立效用确认
        ↓ 通过
阶段三：真实消息被动截获与等价性验证
        ↓ 通过
阶段四：隐藏数据域/敏感属性推断
        ↓
阶段五：匿名消息跨时间关联
        ↓
阶段六：控制实验、统计检验和最终判决
```

阶段一或阶段二失败时立即结束，不运行攻击；不能用“既然已经准备了攻击代码”作为继续理由。

---

## 4. 阶段一：官方完整 Latte 效用复现

### 4.1 数据准备优先级

优先下载作者公开的缓存图像特征和文本特征，因为这样最容易与官方结果匹配。只有作者缓存不可用时，才从官方完整原始数据重新计算特征。

不得使用传统的每种扰动只有10000张的 CIFAR-100-C 代替 `CIFAR-100-C-Full`。

### 4.2 运行前硬检查

程序启动后先输出并断言：

```text
官方提交版本 == 7c2e025...
模型 == ViT-B/16
CIFAR 扰动域数 == 19
CIFAR 每个扰动域特征数 == 60000
CIFAR 客户端数 == 57
CIFAR 所有客户端样本总和 == 60000
VLCS 域数 == 4
VLCS 客户端数 == 40
VLCS 所有客户端样本总和 == 10729
```

注意：CIFAR 的60000张是经官方特殊划分后分配到57个客户端，总和仍为60000，不是 `60000 × 57`。

任何一项不满足就停止运行，并标记为“数据/配置无效”，不能生成研究结论。

### 4.3 禁止截断测试流

删除或禁用上次实验中的：

```text
utility_count_cifar = 64
utility_count_vlcs = 48
每客户端固定只取48/64张
```

必须让官方服务器循环自然运行至所有客户端 `is_done()`。

### 4.4 官方基线

对两个数据集分别运行：

1. 不进行适应的基础模型；
2. 完整 Latte。

先运行随机种子0作冒烟检查，确认总样本数和运行流程；通过后运行随机种子0、1、2、3、4。

官方命令逻辑为：

```bash
# CIFAR-100-C-Full
python main.py --dataset CIFAR100CFull --num_clients_per_domain 3 --model ViT-B/16 --algo no_adapt --seed <0..4> --cuda
python main.py --dataset CIFAR100CFull --num_clients_per_domain 3 --model ViT-B/16 --algo latte    --seed <0..4> --cuda

# VLCS
python main.py --dataset VLCS --num_clients_per_domain 10 --model ViT-B/16 --algo no_adapt --seed <0..4> --cuda
python main.py --dataset VLCS --num_clients_per_domain 10 --model ViT-B/16 --algo latte    --seed <0..4> --cuda
```

实际路径可按服务器调整，但参数和值不得改变。

### 4.5 官方复现门槛 R1

必须同时满足：

1. 五个种子的平均结果中，Latte 在两个数据集上都优于不适应模型；
2. CIFAR-100-C-Full 的 Latte 均值与39.51%的差距不超过1.0个百分点；
3. VLCS 的 Latte 均值与82.57%的差距不超过1.0个百分点；
4. 不适应模型也应分别接近38.91%和80.83%，差距不超过1.0个百分点；
5. 每个数据集至少4/5个种子的 Latte 提升方向为正；
6. 使用完整样本数，而不是截断测试流。

如果只满足“Latte 比基础模型好”，但绝对数值明显偏离论文结果，仍应先检查缓存、文本模板、数据版本和数据划分，不能直接进入攻击。

如果用作者公开缓存、固定提交和完整官方命令仍无法满足 R1，最终结论为全面归档。

---

## 5. 阶段二：确认收益确实来自跨客户端消息

### 5.1 为什么还需要本地记忆对照

论文中的 Latte 同时包含：

- 客户端自己的本地记忆；
- 其他客户端提供的外部原型。

Latte 优于不适应模型，不一定说明跨客户端协作本身有效。因此必须增加一个只关闭外部消息、保留全部本地逻辑的对照。

### 5.2 三种系统

|系统|本地记忆|其他客户端原型|作用|
|---|---:|---:|---|
|不适应模型|否|否|基础参考|
|只使用本地记忆|是|否|排除本地适应收益|
|完整 Latte|是|是|测量跨客户端消息的真实贡献|

“只使用本地记忆”必须由官方 `LatteClient` 实现，仅将配置中的 `global.enabled` 设为 `False`，其余参数和数据顺序完全不变。该结果应明确标记为“受控消融”，不能说是官方脚本直接提供的基线。

### 5.3 配对比较

对每个种子、每个客户端保存：

- 只使用本地记忆的正确数和总数；
- 完整 Latte 的正确数和总数；
- 两者对每一张图片的预测；
- 两者的预测差异；
- 客户端所属域。

在同一客户端和同一测试流上做配对比较，不能只比较两个全局平均数。

### 5.4 跨客户端效用门槛 R2

必须同时满足：

1. 在至少一个数据集上，完整 Latte 相对只使用本地记忆平均提高至少0.5个百分点；
2. 该数据集至少4/5个种子的提升方向为正；
3. 以客户端为重采样单位的配对95%置信区间下界大于0；
4. 另一个数据集上平均下降不得超过0.2个百分点；
5. 不存在某一数据域或大量客户端系统性坍缩；
6. R1 已通过。

这里的0.5个百分点在运行前固定，不得看完结果后降低。R2 失败时，说明真实跨客户端消息没有提供足够稳定的自有实验效用，D1-F 全面归档。

---

## 6. 阶段三：截获真实消息且不得改变 Latte

### 6.1 截获对象

必须直接记录官方 Latte 的：

- 客户端 `upload_cache()` 输出的每类原型和相应不确定性；
- 服务器 `subset_selection()` 选中的客户端索引，仅作为私有诊断真值；
- 客户端 `download_cache()` 收到的外部原型和不确定性。

不得在外部重新计算“类似的平均特征”代替真实上传消息。

### 6.2 可见字段划分

#### 攻击者可以看到

- 上传的真实原型张量；
- 上传的不确定性张量；
- 消息轮次和时间；
- 实际协议中无法隐藏的消息形状与字节数。

#### 只用于评测，攻击者不能看到

- 客户端真实编号；
- 客户端真实数据域；
- 原始图片索引；
- 真实标签和类别比例；
- 同一客户端配对标签；
- 服务器选择来源客户端的真实索引。

### 6.3 文件隔离

```text
wire_payload.npz          # 真正传输的原型与不确定性
public_header.jsonl       # 轮次、时间、形状、字节数
private_diagnostics.jsonl # 客户端、域、样本索引等评测真值
```

攻击器的输入加载器不得导入私有诊断文件；私有诊断文件只用于生成攻击标签和统计分组。

### 6.4 被动记录等价性 R3

固定同一数据、同一种子，分别运行：

1. 完全未加记录功能的官方 Latte；
2. 加被动消息记录功能的 Latte。

必须满足：

- 所有预测类别完全一致；
- 最大输出数值差不超过 (10^{-4})；
- 每个客户端读取的样本顺序一致；
- 记录消息数量与官方同步次数一致。

R3 失败只能修复记录代码并原样重跑，不能继续攻击。

---

## 7. 阶段四：攻击一——推断隐藏数据域或敏感属性

### 7.1 问题定义

攻击者看见客户端上传的真实类别原型，但不知道客户端面对的隐藏数据域或敏感分布属性。攻击目标是根据消息推断该属性。

不能把“猜客户端编号”作为主要结果，因为服务器通常本来就知道消息来自哪个客户端。

### 7.2 主要任务：VLCS 隐藏域推断

VLCS 有4个域，每个域10个客户端。对每个域固定：

- 6个客户端用于训练攻击器；
- 2个客户端用于选择攻击器参数；
- 2个客户端用于最终测试。

训练、验证和测试客户端完全不重叠。五个随机种子轮换客户端分组。

随机猜测的平衡准确率为25%。

### 7.3 外部验证：CIFAR 扰动家族推断

将19种扰动按论文常用方式分成：

- 噪声；
- 模糊；
- 天气；
- 数字处理。

在每个具体扰动的3个客户端中：

- 1个作为攻击训练客户端；
- 1个作为验证客户端；
- 1个作为测试客户端。

测试客户端训练时从未出现。为了避免攻击器只记住某个具体扰动，还应补充“留出具体扰动”的困难设置：训练时不出现某一具体扰动，但测试时仍需判断其所属扰动家族。

### 7.4 攻击输入

分别运行：

1. 只看真实消息数值；
2. 只看公开消息头；
3. 同时看消息数值和公开消息头。

主结论必须由“只看真实消息数值”支持。

### 7.5 攻击器

只允许使用预先固定的简单方法：

- 最近类别中心；
- 逻辑回归；
- 线性支持向量机。

每个方法使用相同的参数搜索预算。主结果取验证集预先选出的单一攻击器，不能在测试集上挑最高数字。

### 7.6 时间隔离

每个客户端的完整测试流按顺序划分：

- 前25%：生成攻击训练阶段消息；
- 中间25%：生成验证阶段消息；
- 后50%：生成未来测试消息。

攻击训练和测试阶段输入图像索引不得重叠。需要记录：

```text
train_image_index_hash
validation_image_index_hash
future_image_index_hash
pairwise_overlap_count = 0
```

注意：Latte 的记忆会跨时间保留，这是被审计系统本身的性质；必须区分“新输入图像不重叠”和“内部记忆状态连续”。

### 7.7 指标

- 平衡准确率；
- 各域平均F1分数；
- 混淆矩阵；
- 以客户端为重采样单位的95%置信区间；
- 五个随机种子的原始结果。

---

## 8. 阶段五：攻击二——匿名消息跨时间关联

### 8.1 问题定义

假设消息中的客户端编号被去除。攻击者输入两个不同时段的消息集合，判断它们是否来自同一个客户端。

这个任务是“同一客户端/不同客户端”二分类，不是猜具体客户端编号。

### 8.2 未见客户端测试

- 攻击器只在辅助客户端上训练；
- 最终测试使用训练阶段从未出现过的新客户端；
- 正负消息对数量相同；
- 困难负样本优先选择同一数据域的不同客户端；
- 两段消息对应的新输入图像不重叠。

### 8.3 固定攻击训练预算

无论最后观察多少条消息，攻击训练阶段始终固定：

- 每个辅助客户端8条消息；
- 相同数量的正负消息对；
- 相同训练轮数；
- 相同参数搜索预算。

测试时分别聚合：

\[
q\in\{1,2,4,8\}
\]

条消息，再判断是否来自同一客户端。这样衡量的是观察更多历史消息是否增强关联能力，而不是给攻击器更多训练数据。

### 8.4 输入表示

优先使用透明的消息对特征：

- 两条原型的余弦相似度；
- 差值绝对值；
- 每类原型相似度的均值、方差和最大值；
- 不确定性差值。

不先使用大型神经网络攻击器。

### 8.5 指标

- 曲线下面积；
- 平衡准确率；
- 误报率1%时的检出率；
- 以客户端为重采样单位的95%置信区间；
- (q=1,2,4,8) 的风险曲线。

---

## 9. 阶段六：必要控制实验

### 9.1 随机标签控制

随机打乱隐藏域标签或同客户端配对标签后：

- 四分类域推断应回到约25%；
- 二分类消息关联应回到约50%。

### 9.2 公开消息头控制

单独使用轮次、时间、消息大小等公开字段。如果仅消息头就能完成攻击，应将结果称为“通信侧信道泄露”，不能归因于原型内容。

### 9.3 简单尺度控制

只使用原型范数、不确定性均值和非零类别数量等简单统计。随后对每个客户端内部做标准化，检查攻击是否只利用尺度差异。

### 9.4 类别槽位控制

Latte 上传的是按类别排列的原型矩阵。对所有消息使用同一随机类别行置换，检查泄露是否主要来自类别槽位；再对每条消息使用独立置换，检查向量内容是否仍保留域信息。

### 9.5 样本成员推断负对照

只选一个代表配置再次确认样本级成员推断接近随机，不再扫描聚合数量，也不把该结果作为 D1-F 成败标准。

---

## 10. D1-F 预注册判定门槛

### G1：真实方法与跨客户端收益成立

- R1 官方完整复现通过；
- R2 跨客户端效用门槛通过；
- R3 被动记录等价性通过。

### G2：至少一种有意义的指纹攻击成立

以下至少一项成立：

1. VLCS 未见客户端的隐藏域推断，纯消息数值平衡准确率 ≥65%；
2. 未见客户端的匿名跨时间关联，纯消息数值曲线下面积 ≥0.65。

同时要求以客户端为重采样单位的95%置信区间下界 >55%。

### G3：排除实验泄漏和简单元数据

必须全部满足：

- 攻击训练客户端与测试客户端完全不同；
- 新输入图像在不同时间阶段无重叠；
- 攻击训练消息数量固定；
- 随机标签控制回到随机水平；
- 主要结果不依赖客户端编号、域名称或私有诊断字段；
- 纯消息结果明显高于只看消息头的结果，或者明确把侧信道作为独立发现。

### G4：具有外部有效性

至少满足一种：

- 隐藏域推断在 VLCS 和 CIFAR 扰动家族上都成立；
- 隐藏域推断与匿名跨时间关联两种任务都成立。

### 最终判定

```text
G1、G2、G3、G4 全部通过
    → D1-F 正式立项
    → 下一阶段研究指纹形成机制和防护方法

任意关键门槛失败
    → A2 / D1 / D1-F 全面归档
    → 不再切换 CoLA，不再追加实验
    → 进入新选题
```

本次不保留“暂时通过”“再换一个数据集”“再调一次攻击器”等中间结论。

---

## 11. 推荐输出目录

```text
outputs/d1_f_YYYYMMDD/
├── decision.md
├── preregistration.json
├── official_reproduction/
│   ├── repository_url.txt
│   ├── repo_commit.txt
│   ├── environment.yml
│   ├── dataset_assertions.json
│   ├── seed_0.json
│   ├── seed_1.json
│   ├── seed_2.json
│   ├── seed_3.json
│   ├── seed_4.json
│   └── reproduction_summary.json
├── collaboration_ablation/
│   ├── per_sample_predictions.npz
│   ├── per_client_metrics.json
│   └── paired_bootstrap.json
├── hook_equivalence/
│   └── equivalence.json
├── messages/
│   ├── wire_payload.npz
│   ├── public_header.jsonl
│   ├── private_diagnostics.jsonl
│   └── message_manifest.json
├── attacks/
│   ├── hidden_domain/
│   │   ├── grouped_client_split.json
│   │   ├── predictions.npz
│   │   ├── random_label_control.json
│   │   └── metrics.json
│   ├── temporal_linking/
│   │   ├── auxiliary_target_clients.json
│   │   ├── pair_manifest.json
│   │   ├── predictions.npz
│   │   ├── random_label_control.json
│   │   └── metrics.json
│   └── sample_membership_negative_control/
│       └── metrics.json
└── figures/
```

---

## 12. `decision.md` 必填模板

```text
官方仓库与提交：
是否使用作者完整缓存：
CIFAR 总评测样本数：
VLCS 总评测样本数：

五种子官方复现：
  CIFAR 基础模型：
  CIFAR Latte：
  VLCS 基础模型：
  VLCS Latte：
R1：通过 / 失败

完整 Latte 相对只使用本地记忆：
  CIFAR 平均增益及客户端置信区间：
  VLCS 平均增益及客户端置信区间：
R2：通过 / 失败

被动记录最大输出差：
预测是否完全相同：
R3：通过 / 失败

隐藏域推断：
  纯消息：
  仅消息头：
  客户端置信区间：

匿名跨时间关联：
  q=1：
  q=2：
  q=4：
  q=8：
  客户端置信区间：

随机标签控制：
客户端是否隔离：
新输入图像是否隔离：
攻击预算是否固定：

G1：通过 / 失败
G2：通过 / 失败
G3：通过 / 失败
G4：通过 / 失败

最终决定：D1-F 正式立项 / A2-D1-D1-F 全面归档
```

---

## 13. 执行时间表

### 第1天：数据与官方命令核验

- 固定仓库版本；
- 下载作者缓存；
- 验证60000/10729总样本数；
- 运行随机种子0的基础模型与 Latte。

### 第2–3天：五种子官方完整复现

- 完成两个数据集、五个种子；
- 对照论文锚点；
- R1 失败则结束。

### 第4天：本地记忆对照与记录等价性

- 只关闭外部消息；
- 完成客户端配对比较；
- 完成记录钩子等价性；
- R2/R3失败则结束。

### 第5–7天：两项攻击

- 未见客户端隐藏域/属性推断；
- 未见客户端匿名跨时间关联；
- 严格固定攻击训练预算。

### 第8–9天：控制与统计

- 随机标签；
- 仅消息头；
- 类别槽位置换；
- 客户端分组置信区间；
- 样本成员推断负对照。

### 第10天：最终判决

- 填写 `decision.md`；
- 不增加新条件；
- 通过则另写正式项目方案；
- 失败则归档并切换新选题。

---

## 14. 通过以后也还不能直接投稿

本实验通过只代表“新问题值得立项”，不是论文已经完成。后续完整论文仍需：

1. 解释指纹来自类别槽位、域方向、不确定性还是长期记忆；
2. 扩展到第二种真实协作 TTA 消息；
3. 提出一种降低指纹、同时保留协作收益的防护机制；
4. 建立隐私—效用—通信三者的完整比较；
5. 在更大数据或实际敏感场景上验证。

如果本实验不通过，就不进行上述工作。

---

## 15. 最终纪律

本次运行不是为了把攻击准确率“调高”，也不是为了证明之前投入没有白费，而是为了回答一个明确问题：在可复现、有效的真实协作测试时适应系统中，是否存在具有实际意义、能够跨客户端和跨时间泛化的消息指纹。如果严格实验不支持它，就接受结论并立即换题。

