# P3 探针：反复曝光方向有没有肉（可行性门槛实验）

> 性质：可行性门槛探针。P1 已证明“泄露随曝光次数上升”（1遍0.51 → 10遍0.59）。P3 要回答的是更关键的问题：**这个泄露是否与“具体组件/机制”相关（存在不对称性），还是只是“看多了就记住”的平凡函数？** 只有前者成立，这个方向才值得投入主实验。
> 前置：先读 Pilot / P1 / P2 三份文档。全部复用同一切分、同一攻击脚本、同一源模型。
> 状态：v1（待跑）。预计 0.5~1 天。

## 0. 一句话

> 如果“只更新 BN 运行统计、不学 affine”怎么反复看都不泄露，而“带 affine / 带记忆库回放”一反复就泄露 → 说明泄露不是“曝光”的平凡后果，而是**特定组件把个体焊进去的**——这个“组件不对称性”就是论文的新意所在。

## 1. 要跑的表（锚点行已有数据，新行约 4~6 个）

| # | 系统 | 说明 | 预期 |
|---|---|---|---|
| 0 | Source | 锚点 | 0.502 |
| 1 | Tent 1遍 BN+affine | 锚点（Pilot） | 0.510 |
| 2 | Tent 10遍 BN+affine | 锚点（P1） | 0.590 |
| 3 | Tent 10遍，只动 BN 运行统计（affine 冻结） | 新 | 若 ≈0.5 → 不对称性出现 |
| 4 | BufTTA 强回放（每流 batch 回放 1 次 ≈32 次） | 新（修 P2） | 若 ≥0.58 → 真实记忆库方法也泄露 |
| 5 | Tent 10遍 + 逐样本裁剪（C=1） | 新（防御行） | 若 AUC 明显回落且 Top-1 不掉 → 便宜防护成立 |
| 6 | （可选）Tent 10遍，按类分桶 AUC | 新 | 需不均衡成员流才有区分度，先不强求 |

> 指标统一用 score_ent AUC（主）；score_conf 一并记录；score_ce 只作诊断。每行同时报 eval Top-1，防坍缩假象。
## 2. 各新行的实现要点

### 第 3 行：Tent 10遍，只动 BN 运行统计（affine 冻结）
```python
# 与 P1 的 10 遍相同，但：
for name, p in model.named_parameters():
    p.requires_grad = False          # 全程不学任何参数
# 前向仍走 model.train()：BN 的 running mean/var 会自动按 batch 统计更新
# 不做 optimizer.step()，只靠 running 统计的反复更新来适应
```
- 这一行把 P1 第 2 行的“affine 学习”拿掉，其余全同；
- 若它 AUC ≈0.5 而第 2 行 0.59 → 泄露来自 affine 参数的学习，而不是 BN 统计本身。

### 第 4 行：BufTTA 强回放（修 P2）
```python
# 与 P2 相同，但把 replay_every 从 16 改成 1：
# 每个流 batch 处理完后，都从 buffer 抽 64 个做一次 CE(伪标签) 回放
# 流共 32 个 batch → 回放约 32 次（不再是 P2 的 2 次）
```
- 若模型坍缩（Top-1 暴跌），把回放改为每 2 个流 batch 一次（约 16 次）再试；
- 记录 replay_batches 实际次数与 buffer 最终规模。

### 第 5 行：Tent 10遍 + 逐样本裁剪（C=1）
```python
# 与 P1 的 10 遍相同，只是反传前对每个样本的梯度做 L2 裁剪：
# g_i = g_i / max(1, ||g_i||_2 / C)，C=1
```
- DP-TTA 报告过“裁剪本身不伤精度甚至提精度”，若这里同时压住 AUC → 几乎免费的防御，是论文的高价值发现。

### 第 6 行（可选，先不做）：按类分桶
- 现成员流每类约 20 张，n_c 差异太小，分桶看不出稀有类效应；
- 要做需先造“不均衡成员流”（部分类只有 1~5 张成员），属于主实验内容，门槛阶段不强求。
## 3. 判读（go / no-go）

| 结果组合 | 结论 |
|---|---|
| 第 3 行 ≈0.5 且 第 2 行 ≥0.58（不对称性成立） | 方向有肉：论文新意 = “泄露由特定组件（affine 学习/记忆库回放）造成，纯 BN 统计不会”——继续 |
| 第 4 行 ≥0.58 且 Top-1 正常 | 方向更稳：泄露不只在人工多遍里，真实记忆库方法也会发生 |
| 第 5 行 AUC 明显回落且 Top-1 不掉 | 高价值：便宜的逐样本裁剪就能防，几乎免费 |
| 第 3 行也 ≈0.59（BN 统计本身就漏） | 不对称性不成立，只剩“曝光次数”这条平凡线 → 放弃 A1 族 |
| 全部 ≈0.5（强回放也不漏） | 反复曝光也不漏 → 单机线彻底结束，转 A2 / 换题 |
| 某行 Top-1 暴跌（<20） | 坍缩，先修该行设置再读数，不算结论 |

## 4. 输出与汇报表（跑完回填）

| # | 系统 | score_ent AUC | score_conf AUC | Top-1 | 备注 |
|---|---|---|---|---|---|
| 0 | Source | 0.5023 | 0.5029 | 11.7 | 锚 |
| 1 | Tent 1遍 | 0.5096 | 0.5126 | 41.2 | 锚 |
| 2 | Tent 10遍 | 0.5895 | 0.5915 | 49.7 | 锚 |
| 3 | Tent 10遍 BN-only | ? | ? | ? | 关键行 |
| 4 | BufTTA 强回放 | ? | ? | ? | 关键行 |
| 5 | Tent 10遍 + 裁剪 C=1 | ? | ? | ? | 防御行 |
| 6 | （可选）按类分桶 | - | - | - | 暂缓 |

结果存到 outputs/p3_seed0/，每行一个 json。

## 5. 跑之前 checklist

- [ ] 复用 seed0 split_indices.npz 与打分脚本，不重新切分
- [ ] 先跑锚点行 0/1/2 确认环境没变（可选，直接引用已有数字也行）
- [ ] 第 3 行确保 affine 全冻结、只靠 BN running 统计更新
- [ ] 第 4 行确认 replay_batches≈32（不再是 2）
- [ ] 每行记录 eval Top-1 防坍缩假象
- [ ] 时间不够时按优先级：3 > 4 > 5
